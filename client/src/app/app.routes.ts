import { Routes } from '@angular/router';
import { ContactListPageComponent } from './features/contacts/pages/contact-list-page/contact-list-page.component';

export const routes: Routes = [
  { path: '', component: ContactListPageComponent },
];
